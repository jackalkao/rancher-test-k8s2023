name: Hello Action

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello message
        run: echo "Hello Github Action"

      - name: Get latest commit SHA
        run: echo "The latest commit SHA is ${GITHUB_SHA}."

      - name: list file
        run: |
          git clone https://github.com/jackalkao/rancher-test-k8s2023.git
          ls stp-devops-helm/be-app-values/values/
